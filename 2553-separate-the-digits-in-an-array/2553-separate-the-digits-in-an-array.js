/**
 * @param {number[]} nums
 * @return {number[]}
 */
var separateDigits = function(nums) {
    
    return nums.join("").split("").map(e => Number(e))
    // nums = nums.map(num => String(num))
    
    
    for (let stringnum of nums){
        
        if (stringnum.length >= 2){
            
        }
        
    }
}