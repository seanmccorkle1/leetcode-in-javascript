var merge = function(array1, length1, array2, length2) {

    array1
    array1.length = length1
    array1

    // array2
    // array2.length = length2
    // array2


    // ...array when an array is inside a function

    array1.push(...array2)
    array1
            
    array1.sort((a,b)=>a - b)

}
